import { Routes } from '@angular/router';
import { MobileSyncComponent } from './mobile-sync.component';

export const MobileSyncRoutes: Routes = [
    { path: '', component: MobileSyncComponent },
];
